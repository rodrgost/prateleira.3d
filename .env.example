# ID do cliente Google OAuth 2.0 (obrigatório para sincronização com o Google Drive)
# Crie no Google Cloud Console: APIs & Serviços > Credenciais > ID do cliente OAuth (Aplicativo da Web)
VITE_GOOGLE_CLIENT_ID=
